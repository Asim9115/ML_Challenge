"""
Inference — run the trained pipeline on the test set and write output TSVs.

Produces:
  - output/matching_results.tsv
  - output/candidate_pairs.tsv
"""

# TODO: implement
# - Load test sources (S1, S2, S3)
# - Preprocess
# - Run blocking → candidate pairs
# - Extract features for candidates
# - Run trained model → predictions
# - Apply threshold
# - Write matching_results.tsv (every S1 entity gets a row, empty if no match)
# - Write candidate_pairs.tsv (every S1 entity gets a row)
# - Validate output format
